# Arduino build output
build/
*.hex
*.elf

# Captured experiment data -- keep runs out of the repo
*.csv
*.mp4
*.mov
*.webm
# ...but the analysis tool's test fixtures are source, not captured data
!faraday-cv/tests/data/*.csv

# Python (faraday-cv)
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/
.venv/

# Node (the browser unit tests)
node_modules/

# Secrets and editor / OS noise
.env
.env.*
.vscode/
.DS_Store
